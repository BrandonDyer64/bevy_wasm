[package]
build = "build.rs"
edition = "2024"
license = "MIT OR Apache-2.0"
name = "cubes"
publish = false
version = "0.18.0"

[dependencies]
bevy = "0.18.0"
bevy_wasm = {path = "../../../bevy_wasm"}
cubes_protocol = {path = "../cubes_protocol"}
