[package]
edition = "2024"
license = "MIT OR Apache-2.0"
name = "cubes_protocol"
publish = false
version = "123.456.789"

[dependencies]
bevy_wasm_shared = {path = "../../../bevy_wasm_shared"}
serde = {version = "1.0", features = ["derive"]}
bevy_ecs = "0.18.0"
